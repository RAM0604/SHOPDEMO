# render.yaml — tells Render how to run this app.
services:
  - type: web
    name: shopdemo
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
